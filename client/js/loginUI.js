class LoginUI {

    // #form;

    // constructor() {
    //     this.#form = document.querySelectorAll("form input");
    //     // this.#form.classList.remove("hidden");

    //     const inputs = this.#form.map(element => {
    //         if (element.type != "submit") {
    //             return element.id;
    //         }
    //     }); 

    //     this.#form.addEventListener('submit', (e) => {
    //         e.preventDefault();
    //         inputs.forEach(id => {
    //             this.chooseLanguage(id);
    //         });    
    //     });
    // }

    // chooseLanguage(type) {
    //     let language = document.querySelector(`#${type}`);
    //     localStorage.setItem(type, language.value);
    // }

}